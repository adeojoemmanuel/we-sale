specVersion: "v1beta"
serviceId: "weapon-survey"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "weapon-survey-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
